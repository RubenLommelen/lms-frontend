import { CodelabFilterPipe } from './codelab-filter.pipe';

describe('CodelabFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CodelabFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
